import ContactProps from '../../components/ContactProps'
import LayoutProps from '../../components/Layout'


export default function Contact() {
  return (
    <LayoutProps>
        <ContactProps phoneNumber={'(19)99999-9999'} email={'velasaromaticas@velasaromaticas.com.br'} address={'Indaiatuba-SP'}/>
    </LayoutProps>
  )
}
